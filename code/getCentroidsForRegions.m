function C = getCentroidsForRegions(I, R)

    Cx = zeros(size(R));
    Cy = zeros(size(R));

    for i=1:size(I,1)
        for j=1:size(I,2)
            if I(i,j)>0
                for 
            end
        end
    end

end